openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts app
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: This is a documentation of contacts app
tags:
  # теги, що ви будете використовувати
  - name: Students
    description: Operations about contacts
  - name: Auth
    description: Auth operations
servers:
  - url: http://localhost:3000
  - url: //example.com/api/v1 #посилання на задеплоєний сайт
paths:
  /contacts:
    get:
      $ref: ../swagger/paths/contacts/get.yaml
    post:
      $ref: ../swagger/paths/contacts/post.yaml
  /contacts/{contactId}:
    get:
      $ref: ../swagger/paths/contacts/{contactId}/get.yaml #$ref - це спеціальний ключ в YAML файлах, який використовується для посилання на інші частини YAML файлу або на інші файли
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
